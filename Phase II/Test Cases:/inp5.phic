$
    Q) 2 objects A,B are seperated by distance 36m, are
    projected with equal velocities 10^(1/2) with angle 45
    and 135 anticlock. Find out several properties of objects
    before and after collision.   
    
$

    start{
    
    mass m1 = 10.
    mass m2 = 15.
    time t.

    double vel_val.
    theta angle1,angle2.  $ user provides values in terminal -> 45,135 (Given) $

    input : angle1,angle2.
    input : vel_val.        
    $ Given, vel = 10^(1/2). $
    {m1} setv {[vel_val * sin(angle), vel_val * cos(angle)]}.
    {m2} setv {[-1*vel_val * sin(angle), vel_val * cos(angle)]}.
    {m1} setp {[-18,0]}.
    {m2} setp {[18,0]}.
    
    t = {m1} time_to_collide {m2}.
    
    [(t != 1)] {
        {m1} setv {{m1} v_after {t}}.
        {m1} setv {{m1} r_after {t}}.
        
        {m2} setv {{m2} v_after {t}}.
        {m2} setv {{m2} r_after {t}}.

        LE = {m1} collide {m2,e}.
    }
    
    output: "Time taken to collide" + t.
    output: "Radius of curvature at 10s" + roc{10}.
    output: "Distance travelled before collision" 
            + {m1} r_after {t}.
    output: "Angle at t = 10s" +  {m1} angle_after {10}.
    output: "Energy loss during collision" + LE.
      
    }